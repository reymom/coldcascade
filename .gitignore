node_modules/
out/
cache/
broadcast/
.env
keeper/.venv/
keeper/.cache/
mcp/.cache/
**/__pycache__/
subgraph/build/
subgraph/generated/
app/node_modules/
app/dist/

# the local rehearsal writes its own address book; only a real deployment is committed
deployments/31337.json

# working docs — never shipped; the submission repo holds only what a judge reads
CLAUDE.md
AGENTS.md
.claude/
*.local.md
tape/.cache/
.vercel

# the wasm build tree; the .spkg it produces is committed because it is the artifact
substreams/target/
# prost output; build.rs regenerates it from proto/desk.proto on every build
substreams/src/pb/
