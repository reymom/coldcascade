# app

The page. Three surfaces, built in this order: the landing (L1 book next to the desk's quote, fed
by the subgraph), Provide (Privy wallet, approve, `ship()` of the canonical program from the
maker's own wallet), Take (one swap through the official router). And the Oct-10 tab, which reads
`results/oct10_replay.csv`.

Not started. Types in `src/types.ts` mirror the contracts.
